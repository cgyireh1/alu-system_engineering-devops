##SSH

Tasks

0. Use a private key----Write a Bash script that uses ssh to connect to your server
                          using the private key ~/.ssh/school with the user ubuntu.

1. Create an SSH key pair----Write a Bash script that creates an RSA key pair.

2. Client configuration file---Your machine has an SSH configuration file for the local SSH client,
 let’s configure it to our needs so that you can connect to a server without typing a password.

3. Let me in!----Add the SSH public key below to your server so that we can connect using the ubuntu user.
